#version 330 core
out vec4 FragColor;



in vec3 FragPos;  
uniform bool selected;
void main()
{

    if(selected)
      FragColor = vec4(0.7, 0.7, 0.3, 1.0);
    else
    FragColor = vec4(0.5, 0.5, 0.5, 1.0);
}